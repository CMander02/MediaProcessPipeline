{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allow the local MediaProcessPipeline web UI to inspect and manage the desktop-owned backend process.",
  "windows": ["main"],
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "allow-backend-get-status",
    "allow-bootstrap-get-preflight",
    "allow-backend-get-logs",
    "allow-backend-start",
    "allow-backend-stop",
    "allow-backend-restart",
    "allow-backend-retry",
    "allow-open-logs",
    "allow-open-external-url",
    "dialog:allow-open"
  ]
}
