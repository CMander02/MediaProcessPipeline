[project]
name = "template"
version = "0.1.0"
description = "Full-stack template: FastAPI + Agentic AI backend, Vite + shadcn-ui + AI SDK frontend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = ["backend"]
